function countSteps(targetSteps) {
    let totalsteps = 0;
    while(totalsteps < targetSteps){
      totalsteps++
    }
    return totalsteps;
    }


    let result = countSteps(50)
    console.log(result);